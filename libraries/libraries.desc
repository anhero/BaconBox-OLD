#!/bin/bash
#Libraries to build


libraries+=( 'zlib' )
libraries+=( 'freetype' )
libraries+=( 'libpng' )

if [ ${libs_target-} != "ios" ]; then
	libraries+=( 'libogg' )
	libraries+=( 'libvorbis' )
	#libraries+=( 'libvorbis-tremor' )
fi

if [ ${libs_target-} != "ios" -a ${libs_target-} != "android" ]; then
	libraries+=( 'sdl' )
	libraries+=( 'sdl_mixer' )
fi

libraries+=( 'sigly' )
libraries+=( 'jsonbox' )

